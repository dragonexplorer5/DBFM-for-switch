#ifndef CJSON_COMPAT_H
#define CJSON_COMPAT_H

#include <stddef.h>


// Parse JSON string into a cJSON root (will copy input)
// Get object item (string) by key (case-sensitive)

#ifndef CJSON_COMPAT_H
#define CJSON_COMPAT_H

/* cjson_compat.h disabled. The project uses third_party/cJSON.* as the canonical
	JSON implementation. */

#endif
#endif