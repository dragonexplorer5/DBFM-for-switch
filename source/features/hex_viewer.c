#include "../ui.h"
#include <stdio.h>

// Hex viewer helper exists in ui.c (show_hex_viewer). This is a placeholder for future separation.
void feature_hex_viewer_init(void) { (void)feature_hex_viewer_init; }
